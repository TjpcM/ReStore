import { Typography } from "@mui/material";

export default function CheckoutPage(props:any){
    return(
        <Typography variant='h3'>
            Only logged users should be able to see this!
        </Typography>
    )
}